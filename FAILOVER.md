# 节点故障自动切换：设计、实测与验证

> 本文所有机制与数字都来自**实测**（脚本已删除，结论保留）。
> 实现见 `lib/watchdog.js`、`lib/api.js`、`lib/kernel.js`、`lib/state.js`。

## 0. 前提：插件不持有流量

```
proxy curl ...  →  HTTPS_PROXY=http://127.0.0.1:17890  →  curl 直连 mihomo
                                                            ↑
                                        插件不在这个链路上，看不到任何请求
```

所以插件**无法**对单个请求重试、改路由或兜底超时。"自动换节点"只能用两件事表达：
**观察内核** 与 **调用内核 API 改变它的选择**。

---

## 1. 实测结论（含被推翻的假设）

### 1.1 健康数据只在 provider 端点

这是本次最重要的纠正。**全局 `/proxies` 不反映 provider 节点的健康**：

| 端点 | 结果 |
| --- | --- |
| `GET /providers/proxies/sub` | ✅ `alive=26 / dead=4`，含真实延迟（53ms / 99ms / 109ms） |
| `GET /proxies`（全局） | ❌ 30 个 provider 节点 `alive` 恒为 `undefined` |

**推论**：第一版实现读全局视图，因此永远看不到节点健康 —— 会被此纠正。

### 1.2 `alive: true` 不是健康证明

刚启动时 provider 报告 **30 个节点全部 `alive: true`，但 `extra` 为空**（从未测过）。
若把它当成健康，一个节点全不可用的订阅看起来会完全正常。

**判定规则（三态）**：

| 状态 | 条件 |
| --- | --- |
| `alive` | `alive !== false` **且** 有正的实测延迟 |
| `dead` | `alive === false`，或最近一次延迟为 `0` |
| `untested` | 没有任何实测记录 |

### 1.3 `/group/{g}/delay` 不写健康数据

实测：它返回 26 条实测延迟、并且**确实改变了选择**，但**每个节点的 `alive` 仍是 `undefined`**。
所以"启动时消除未知"必须调用 **`healthcheckProvider`**，不能用 `/group/delay`。

### 1.4 钉住需要节点属于该组

`PUT /proxies/{g} {name}` 在节点**不是该组成员**时返回
`400 Selector update error: proxy not exist`。

provider 健康覆盖订阅里的全部节点，而一个组可能只暴露子集 ——
所以选候选时必须与该组的 `all` 求交集。

### 1.5 `url-test` 组的 `fixed` 与 `now` 可以不一致

对 `url-test` 组 PUT 会设置 `fixed`，但 `now` 可能仍指向最快节点。
这意味着"钉住"对自动组只是偏好，不是强制 —— 因此实现里的解除钉住逻辑是必要的。

### 1.6 各操作的实测成本

| 操作 | 耗时 | 用途 |
| --- | --- | --- |
| `GET /proxies` | **8ms** | 每次轮询都读（拿 `now` / `fixed` / 成员） |
| `GET /providers/proxies/{p}` | 小 | 每次轮询都读（拿健康） |
| `GET /group/{g}/delay` | **4–5s** / 28 节点 | 强制重测（升级时才用） |
| `PUT /providers/proxies/{p}` | **747ms** | 重拉订阅 |
| `GET /providers/proxies/{p}/healthcheck` | **8–9s** / 30 节点 | 强制全量测速 |
| `PUT` / `DELETE /proxies/{g}` | 快 | 钉住 / 解除 |

成本极不对称，因此轮询便宜的操作、只在升级时才用昂贵的。

---

## 2. 四层防御（已实现）

### 第 1 层：启动测速（`prime()`）

内核起来后调用一次 `healthcheckProvider`，把 `untested` 清零。

**实测效果**：`untested=30` → `alive=26 / dead=4`（另一轮：`20/10`）。
同时让 `url-test` 的首次选择基于实测，而不是随机选中一个从未测过的节点。

**若不测速**：每 20s 轮询一次、直到某个组自己的 `interval`（300s）到点，期间所有节点
都是 `alive: true` 但无延迟 —— 看起来健康，实际可能全挂。

### 第 2 层：看门狗（`tick()`）

每 `pollMs`（默认 20s）读两次（合计约 10ms），只在**当前节点确认为 `dead`** 时升级：

```
当前节点 alive     -> 什么都不做
当前节点 untested  -> 不判死；宽限期（默认 30s）过后才强制测速
当前节点 dead      -> 升级（限流：两次升级间隔 >= escalateMs，默认 60s）
```

升级（`#escalate`）：

1. `GET /group/{g}/delay` 强制重测 —— `url-test` 会自行重选；
2. 重读；若 `now` 已健康 → 完成；
3. 仍未健康 → 从**该组成员中**选最快的存活节点，`PUT` 临时钉住；
4. 组内无存活节点 → 交给第 4 层。

**理由**：第 1、2 步是"让内核自己选"，只有在它选不出来时才越权指定。

### 第 3 层：进程崩溃（`kernel.js`）

监听子进程 `exit`：

1. **立刻写 `DOWN`**（这是最低要求：修复前崩溃后状态仍停在 `UP`，
   `proxy` 命令会继续注入一个没人监听的代理地址，且原因难查）；
2. 有界重启：5s / 15s / 45s，最多三次。连续失败则停在 `DOWN` 并记录日志路径。

重启上限的理由：配置错误、版本不匹配这类问题不会因为重试而消失，
无上限重启只会静默烧 CPU。

### 第 4 层：组内全部节点失效（`recover()`）

1. **先写 `DEGRADED`**（见下方"关键修正"）；
2. `PUT /providers/proxies/{p}` 重拉订阅（机场可能换了 IP 或补了节点）；
3. `GET /providers/proxies/{p}/healthcheck` 强制全量重测；
4. 恢复则回到 `UP`，否则保持 `DEGRADED`；4 层有独立的冷却（`escalateMs × 5`）。

---

## 3. 关键修正：DEGRADED 必须先发布

第一版在 `recover()` **之后**才发布 `DEGRADED`，而 `recover()` 含两次慢调用
（747ms + 8~9s）。实测的日志顺序证明了这个问题：

```
[dead +12015ms] 重新拉取订阅失败       ← 12 秒后才结束
```

在这 10 秒里，shim 仍在注入一个**不可用的代理**，所有 `proxy` 命令都在超时。

修正后（再次实测）：

```
[dead state +3020ms] -> DEGRADED
[dead +3021ms] 全部 1 个节点均不可用，代理命令将回退为直连
[dead +3021ms] 本组所有节点均不可用，正在重新拉取订阅…
```

**一个轮询周期内（约 3s）就降级**，然后才去做耗时的恢复尝试。

---

## 4. 状态机

| 状态 | 含义 | shim 行为 |
| --- | --- | --- |
| `UP` | 内核在跑，组内至少一个可用节点 | 注入代理，正常执行 |
| `DEGRADED` | 内核在跑，但**该组全部节点已确认失效** | **警告 + 直连** |
| `DOWN` | 内核未运行 | 警告 + 直连 |

**`DEGRADED` 的判定条件**：组内 `alive === 0` **且** `dead > 0` **且** `untested === 0`
—— 只要还有未测速的节点，就绝不降级。因为错误的 `DEGRADED` 会静默把流量改走直连。

**唯一会改变流量去向的地方**：`DEGRADED` 下本想走代理的请求会直连。
对被墙的目标不会更糟（直连同样失败），但这是需要知情的一点。

---

## 5. 验证结果

配置在启动前一次生成（与插件真实行为一致：每次启动都重新生成，从不热重载）。

| # | 验证项 | 结果 |
| --- | --- | --- |
| 1 | 启动测速消除"未知" | ✅ `untested 30 → 0`，`alive=26 / dead=4`，延迟 53/99/109ms |
| 2 | 健康时保持 `UP` | ✅ `state=UP now=香港WAP-优化2` |
| 3 | **当前节点失效后自动切换** | ✅ 见下 |
| 4 | 组内全挂 → `DEGRADED` 且**及时** | ✅ `+3020ms`，早于慢恢复 |
| 5 | 内核崩溃 → 立刻 `DOWN` | ✅ 杀进程后 1.5s 内状态为 `DOWN` |

### 第 3 项（核心诉求）的实测输出

`url-test` 组不会自己选中死节点（它选最快），所以"你在用的节点挂了"这个场景用
`select` 组复现：把它显式指向一个订阅自己报告为失败的节点，然后观察。

```
"pickdead" members = 30
dead candidates among members: 4
[3] "pickdead" (select group) -> dead node "加拿大-优化"
    before: now=加拿大-优化
  [pick] 当前节点「加拿大-优化」已失效
  [pick] 重新测速后仍未自动切换，临时指定「香港WAP-优化2」（53ms）
    after : now=香港WAP-优化2 (state=alive)
    => escaped the dead node: YES
    => landed on a healthy node: YES
```

---

## 6. 明确不做

| 不做 | 理由 |
| --- | --- |
| 永久钉住节点 | 钉住即设 `fixed`，组停止自动优化；被钉节点随后死掉就卡住了 |
| 在 JS 里解析/排序订阅节点 | 内核已做；重复的事实来源，订阅格式一变就要改代码 |
| 对单个请求重试/改路由 | **不可能** —— 插件不在流量链路上（第 0 节） |
| 把"未测速"当"失效" | 实测启动时全部节点都未测速；那样会在启动瞬间误判并乱切 |
| 无上限重启内核 | 配置/版本类问题重试不会好，只会静默烧 CPU |
| 降级时阻断命令 | 与既定决策一致：警告 + 照常执行，让国内流量仍可用 |

## 7. 可调参数（当前为固定值）

| 参数 | 值 | 位置 |
| --- | --- | --- |
| 轮询间隔 | 20s | `watchdog.pollMs` |
| 升级限流 | 60s | `watchdog.escalateMs` |
| 未测速宽限 | 30s | `watchdog.untestedGraceMs` |
| 恢复冷却 | 5 × `escalateMs` = 300s | `recover()` |
| 内核重启阶梯 | 5s / 15s / 45s | `kernel.restartDelaysMs` |
| 内核自身测速间隔 | 300s | `config.js` 的 `healthCheckSec` |

这些尚未暴露到设置页；如需可调，再加字段即可。
